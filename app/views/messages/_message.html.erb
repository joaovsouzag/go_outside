<div id="message-<%= message.id %>">
  <small>
    <strong class="white-text"><%= message.user.username %></strong>
    <i class="white-text"><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p class="white-text"><%= message.content %></p>
</div>
