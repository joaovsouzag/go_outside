<%= render "shared/searchbar" %>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-sm-8">
      <div class="cards">
        <div class="location-cards">
            <% @locations.each do |location| %>
              <%= link_to location_path(location), class: "no-deco" do %>
              <div class="location">
                <h4><%= location.name %></h4>
                <p>Address:<%= location.address %></p>
                <p>Type:<%= location.location_type %></p>
                <%# <p><%= location.photos %>
              </div>
            <%end %>
          <%end %>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="sliders">
  <div class="slider-age">
    <p> Age_Group</p>
      <input type="range" min="18" max="100" oninput="rangeValue.innerText = this.value">
    <p id="rangeValue"> years </p>
  <div class="slider-distance">
    <p> Max_Distance <p>
      <input type="range" min="0" max="100" oninput="rangeValue3.innerText = this.value">
    <p id="rangeValue3"> km </p>
  </div>
</div>
